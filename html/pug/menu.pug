doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Document
        link(rel="stylesheet" href="styles.css")

    body
        include ../blocks/header/header
        .content
            //- .content__block
            //-     .container
            //-         .title
            //-             h1 Меню
            .content__block-s
                .container-fluide
                    .store__info
                        .store__info-item
                            | Чтобы сделать заказ позвоните по телефону:
                            br 
                            a(href="tel:+78332416646") 
                                b 41-66-46
                        .store__info-item
                            | Бесплатная доставка при сумме заказа от 
                            b 450 Р
                        .store__info-item
                            | Режим работы - ежедневно 
                            b 10:00 - 22:00 в пределах города
                        .store__info-item
                            | Время доставки уточнит менеджер при заказе
            .content__block
                .container-fluide
                    include ../blocks/store/store

        script(src="https://api-maps.yandex.ru/2.1/?lang=ru_RU")
        script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.2.0/jquery.fancybox.min.js")
        script.
            !function (i,s,o,g,r,a,m) {
                if (typeof window[g] === "undefined") {
                    var script = s.createElement("script");
                    var node   = s.getElementsByTagName("script")[0];
                    script.src = o;
                    script.type = "text/javascript";
                    node.parentNode.insertBefore(script, node);
                }
            }(window, document, "js/jquery.min.js", "jQuery");
        // /подключение jquery
        script(src="/main.js")